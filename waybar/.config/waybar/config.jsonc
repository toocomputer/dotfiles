{
    "position": "top",
    "margin-top": 2,
    "margin-bottom": 1,

    "modules-left": ["custom/arch", "hyprland/workspaces"],
    "modules-center": ["clock"],
    "modules-right": ["cpu", "pulseaudio", "network", "tray"],

    "custom/arch": {
        "format": "",
        "tooltip": false
    },

    "clock": {
        "interval": 60,
        "format": "{:<span color='#cdd6f4'>%I:%M %p</span> <span color='#fab387'>%z</span> <span color='#7c7f93'>|</span> <span color='#b4befe'>%a</span> <span color='#7c7f93'>•</span> <span color='#89b4fa'>%b</span> <span color='#7c7f93'>|</span> <span color='#cba6f7'>%Y-%m-%d</span>}",
        "tooltip": false
    },

    "hyprland/workspaces": {
        "on-click": "activate",
        "persistent-workspaces": {
            "*": 5
        }
    },

    "cpu": {
        "interval": 5,
        "format": "{usage}%",
        "on-click": "xdg-terminal-exec btop"
    },

    "pulseaudio": {
        "format": "{volume}%",
        "format-muted": "muted",
        "on-click-right": "pamixer -t",
        "tooltip-format": "Playing at {volume}%",
        "scroll-step": 5
    },

    "network": {
        "format-wifi": "{essid} ({signalStrength}%)",
        "format-ethernet": "{ifname}",
        "format-disconnected": "disconnected",
        "tooltip-format": "{ifname} via {gwaddr}",
        "max-length": 50
    },

    "tray": {
        "icon-size": 18,
        "spacing": 10
    }
}
